'use strict';

var config = {};

config.aws = {};

config.aws.region = '';
config.aws.poolId = '';
config.aws.clientId = '';
config.aws.identityPoolId = '';

config.aws.dynamodb = {};
config.aws.dynamodb.suppression = '';

module.exports = config;
